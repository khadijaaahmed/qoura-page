<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>main-page</title>
     <link rel="stylesheet" href="/public/style.css"/>
</head>
<body>
    <h1 class="h1">Qoura</h1>
    <div class="div">
      <% for (let post of posts) { %>
        <h4 class="username"><%= post.username %></h4>
        <h4 class="content"><%= post.content %></h4>
        <h4 class="email"><%= post.email %></h4>
        <a href="http://localhost:8080/posts/<%=post.id %>">show</a>
        <a href="http://localhost:8080/posts/<%=post.id %>/edit">edit</a>
        <form method="post" action="/posts/<%= post.id %>?_method=DELETE">
          <button>delete</button>
      </form>
        <% } %>
        
    </div>
    <a href="http://localhost:8080/posts/new">click to add</a>
</body>
</html>